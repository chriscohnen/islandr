# 11. Risks and Technical Debt

## 11.1 Risks

Risks are ordered by priority (probability × impact). Each risk references the ADR that created it and the mitigation in Chapter 8 or quality scenario where one exists.

**Probability:** H = High (>50%), M = Medium (10–50%), L = Low (<10%), Permanent = accepted ongoing condition
**Impact:** H = High (data loss, security breach, service outage), M = Medium (degraded functionality), L = Low (minor friction)

### High Priority

| ID | Risk | Prob. | Impact | Priority | Mitigation | Source |
|---|---|---|---|---|---|---|
| R-031 | SQLite used in a team-sized deployment without replication or backup strategy. Single-file DB on a failing disk means data loss. | M | H | **High** | Documentation clearly states team self-hosters should use PostgreSQL. README does not bury this. | [ADR-0004](../adr/0004-sqlite-dev-postgres-prod.md) |
| R-040 | UCG-side firewall rules drift from Islandr's ACL model in v1. No enforcement exists for what UCG actually allows. An admin may change UCG rules manually without updating Islandr. | H | M | **High** | v1 limitation — documented clearly. v2 pull-mode agent closes the gap. Admin Console shows Islandr's computed ruleset, not UCG's actual state. See §8.4 (dashboard shows last-known reload status). | [ADR-0005](../adr/0005-hub-only-firewall.md) |
| R-062 | Backup files (SQLite `.db` snapshots, Postgres dumps) in `plaintext` retention mode contain every peer's private key. Off-host backup exposure is entirely the operator's responsibility. | M | H | **High** | Documented loudly in README.md; the `plaintext` Admin Console banner repeats this point. See §8.2 (no credential storage, least privilege). | [ADR-0007](../adr/0007-private-key-retention.md) |
| R-153 | No renewal reminder for an islandr-managed TLS certificate means it can silently expire, breaking HTTPS for every client with no warning. | M | H | **High** | An expiry-date banner in Settings for the manual (managed/referenced) modes. Closed for the ACME mode specifically by scheduled auto-renewal ([ADR-0019](../adr/0019-acme-hand-rolled-client.md)); remains open for operators who stay on manual certificate management. | [ADR-0015](../adr/0015-builtin-tls-termination.md), [ADR-0019](../adr/0019-acme-hand-rolled-client.md) |
| R-165 | Hand-rolled JWS signing and PKCS#10 CSR DER encoding are security-sensitive, self-maintained code with no upstream security-patch stream, unlike a library dependency. A bug could produce a malformed-but-accepted request, or a subtler cryptographic mistake. | M | H | **High** | Protocol surface deliberately narrow (only what HTTP-01 + Let's Encrypt requires); reuses the already-reviewed DER-encoding pattern from `TlsService`; integration tests run against Let's Encrypt's staging environment before any production-CA code path ships. See [ADR-0019](../adr/0019-acme-hand-rolled-client.md). | [ADR-0019](../adr/0019-acme-hand-rolled-client.md) |

### Medium Priority

| ID | Risk | Prob. | Impact | Priority | Mitigation | Source |
|---|---|---|---|---|---|---|
| R-001 | Native-image reflection/serialization bugs: code that works on JVM breaks on native due to missing reflection config or missing native-image metadata. | M | M | **Medium** | Run integration tests against the native binary in CI, not only the JVM build. Track config in `src/main/resources/META-INF/native-image/`. See §8.3 (integration tests cover native binary). | [ADR-0001](../adr/0001-quarkus-backend.md) |
| R-002 | Native build requires ≥ 6–8 GB RAM. Fails silently on undersized CI runners or local machines. | H | L | **Medium** | Native build runs only on `main` and tags, not on every PR. Builders are ubuntu-latest (≥ 7 GB RAM). | [ADR-0001](../adr/0001-quarkus-backend.md) |
| R-020 | A bug in the ruleset generator produces a syntactically valid but semantically wrong ruleset (e.g. drops all traffic) that passes `nft -c -f`. Validation catches syntax, not intent. | L | H | **Medium** | Integration-test the generator against known peer/role/resource fixtures with golden-file expected rulesets. "Preview ruleset" view in Admin Console before apply. See §8.3 (firewall adapter tests), §8.5 (nft error handling). | [ADR-0003](../adr/0003-nftables-replaces-ufw.md) |
| R-034 | SQLite is not an official Quarkus extension. Native-image friction: reflection config, JNI metadata. May surface as intermittent failures in native builds. | M | M | **Medium** | Validate native build with SQLite as the first build artifact. Tracked as the first thing to fail and fix. Workaround: PostgreSQL if friction is sustained. | [ADR-0004](../adr/0004-sqlite-dev-postgres-prod.md) |
| R-043 | A compromised hub can publish malicious ACL change instructions that the v2 pull-mode agent applies. The hub becomes a vector for reconfiguring the trusted-network firewall. | L | H | **Medium** | v2 agent-side policy (e.g. require operator approval for ACL changes outside an expected diff envelope); audit trail on the agent side independent of the hub. See §8.1 T-001. | [ADR-0005](../adr/0005-hub-only-firewall.md) |
| R-051 | L4-only enforcement misunderstood as L7 enforcement. An admin grants "RDP only" but SSH over port 3389 is not blocked. | M | M | **Medium** | Admin Console labels the protocol field "Anzeige im Portal" (not a security control). Tooltip and docs name the L4/L7 boundary explicitly. | [ADR-0006](../adr/0006-resource-level-acl.md) |
| R-054 | A poorly-scoped all-ports grant silently widens access when admins later add ports to a resource. No explicit admin action is required for the expansion to take effect. | M | M | **Medium** | UI shows "wird automatisch erweitert wenn neue Ports hinzukommen" warning on ⓐ-grants; audit log records "port added to resource X, role Y had ⓐ grant → N new accept rules" with a diff. | [ADR-0006](../adr/0006-resource-level-acl.md) |
| R-060 | DB-file exfiltration exposes peer private keys in `plaintext` retention mode. | M | M | **Medium** | Use `encrypted` retention mode ([ADR-0007](../adr/0007-private-key-retention.md)): AES-256-GCM at rest, key delivered via systemd-creds. A DB-only attacker cannot recover keys without the separate master key. `plaintext` mode shows a permanent Admin Console banner. See §8.2, §8.1 T-007. | [ADR-0007](../adr/0007-private-key-retention.md) |
| R-063 | `GET /peers/{id}/conf` is a re-display endpoint; a stolen admin session cookie grants access to every stored peer `.conf` file in `plaintext` or `encrypted` mode. | L | H | **Medium** | Same authentication requirements as the create endpoint; in v2 the system-role check restricts this to `ADMIN`s only (END_USERs can only re-display their own peers via `/me/peers/{id}/conf`). See §8.2, §8.1 T-002. | [ADR-0007](../adr/0007-private-key-retention.md) |
| R-070 | Hub VM is internet-exposed. Targeted attacker exploiting a vulnerability in the Quarkus HTTP layer or a dependency gets code execution. | L | H | **Medium** | Unprivileged process user (ADR-0011). CVE scanning via Dependabot + CodeQL. Hardening guide (fail2ban, SSH key only, no password auth). See §8.2 (least privilege, CVE tracking), §8.1 T-009. | [ADR-0011](../adr/0011-process-privilege-model.md) |
| R-110 | The fixed nft file path `/var/lib/islandr/ruleset.nft` must be writable only by the `islandr` user. If another process can write to that path, it can inject arbitrary nftables rules via the sudo grant. | L | H | **Medium** | `chmod 700 /var/lib/islandr/`; only `islandr` user owns the directory. See §8.2, §8.1 T-003. | [ADR-0011](../adr/0011-process-privilege-model.md) |
| R-113 | A distro update that changes the path of `nft` or `wg` (e.g. from `/usr/sbin/nft` to `/usr/bin/nft`) silently breaks the sudoers rule, leaving Islandr unable to reload the firewall. | L | H | **Medium** | Deployment script uses `which nft` and `which wg` to verify paths match the sudoers file; CI smoke test calls `sudo -l -U islandr` and asserts expected commands are listed. See §8.5 (startup failure handling). | [ADR-0011](../adr/0011-process-privilege-model.md) |
| R-120 | The 0.11.0 proxy socket `/run/islandr/proxy.sock` must be owned by `islandr:islandr` (mode 0600). If the socket is world-readable, any local process can send proxy commands. | L | H | **Medium** | systemd `RuntimeDirectory=islandr` sets ownership automatically. See §8.2. | [ADR-0012](../adr/0012-docker-socket-proxy.md) |
| R-130 | The default `Everyone` role has auto-membership, so a grant on it reaches all users — present and future. A careless grant (especially ⓐ-all-ports, compounding R-054) silently widens access to every current and future user: a least-privilege footgun. | M | M | **Medium** | `Everyone` is seeded empty and inert until a grant is added deliberately; the ACL matrix labels it "alle Nutzer (auch künftige)" and warns on any grant added to it; grant changes are audited; auto-membership is fixed to exactly one seeded role (no UI to flag arbitrary roles `auto_all`). See §8.2. | [ADR-0013](../adr/0013-default-everyone-role.md) |
| R-151 | In the *referenced* (file-path) TLS storage mode, islandr does not control the filesystem permissions of the path it's pointed at. | M | M | **Medium** | Settings documents that the referenced path's ownership/mode is the operator's responsibility — the same trust boundary a reverse proxy's own certificate file already has today. | [ADR-0015](../adr/0015-builtin-tls-termination.md) |
| R-164 | The HTTP-01 challenge endpoint is a new, permanently-routable, unauthenticated internet-facing surface. | M | M | **Medium** | Serves only a pre-computed, non-secret token string; no attacker-controlled input parsed beyond a path segment compared byte-for-byte against the single currently-expected value; returns 404 for everything else. See §8.1 T-016. | [ADR-0019](../adr/0019-acme-hand-rolled-client.md) |
| R-166 | A missed scheduled renewal (e.g. the hub is down across the entire 30-day renewal window) could let an ACME-managed certificate expire despite ADR-0019's intent to close R-153 for this mode. | L | H | **Medium** | A renewal check also runs on every boot, not only the daily schedule; ADR-0015's expiry-date banner remains a visible backstop for this mode too. | [ADR-0019](../adr/0019-acme-hand-rolled-client.md) |

### Low Priority

| ID | Risk | Prob. | Impact | Priority | Mitigation | Source |
|---|---|---|---|---|---|---|
| R-003 | Long-running `ProcessBuilder` calls for `nft -f` block a Quarkus I/O thread, causing request timeouts under concurrent load. | L | M | **Low** | Run `ProcessBuilder` calls on Quarkus's worker pool, not the I/O thread; bound execution time. See §8.3. | [ADR-0001](../adr/0001-quarkus-backend.md) |
| R-021 | Operators familiar with ufw undo the systemd masking and add conflicting `ufw allow` rules outside Islandr's table. | L | M | **Low** | Document loudly in README.md and in the systemd unit description that ufw is masked deliberately. | [ADR-0003](../adr/0003-nftables-replaces-ufw.md) |
| R-022 | SSH and the Islandr HTTPS port living in a separate `inet filter input` table means two firewall surfaces on the hub VM. An operator mis-editing the input table could lock themselves out. | L | M | **Low** | Ship a documented "minimal input table" template alongside Islandr; deployment guide spells out the boundary explicitly. | [ADR-0003](../adr/0003-nftables-replaces-ufw.md) |
| R-023 | Distros without nftables (older systems, niche derivatives) are unsupported. An operator installs Islandr on an incompatible host and gets opaque failures. | L | L | **Low** | Minimum distro version stated in PRD N-02. Startup check verifies `nft` is present and reachable. | [ADR-0003](../adr/0003-nftables-replaces-ufw.md) |
| R-131 | If the seeded `Everyone` role could be deleted, renamed, or have `auto_all` cleared, the "reaches all users" contract would break silently and existing grants would change meaning. | L | M | **Low** | The role is protected server-side: delete/rename/flag-clear on it return HTTP 409; grants on it stay editable. | [ADR-0013](../adr/0013-default-everyone-role.md) |
| R-030 | "Portable SQL only" constraint erodes: a developer adds a Postgres `JSONB` column or SQLite-specific `CHECK` and breaks the other backend. | L | M | **Low** | Both CI jobs (SQLite + Postgres) must stay green. Rule documented in `CONTRIBUTING.md`. | [ADR-0004](../adr/0004-sqlite-dev-postgres-prod.md) |
| R-032 | Flyway migrations that pass on SQLite may fail on PostgreSQL due to subtle dialect differences (default value syntax, identifier casing, constraint syntax). | L | M | **Low** | Flyway runs in CI on both backends; migrations are tested, not assumed portable. | [ADR-0004](../adr/0004-sqlite-dev-postgres-prod.md) |
| R-033 | The activity-samples table approaches a write-rate concern on SQLite with mid-hundreds of peers (30s polling × N peers × 30 days). | M | L | **Low** | Documented limit; operators near the limit migrate to PostgreSQL. See §8.4 (activity samples retention). | [ADR-0004](../adr/0004-sqlite-dev-postgres-prod.md) |
| R-035 | A future contributor adds a dependency under an EUPL-incompatible license; the combined work enters a license conflict. | L | M | **Low** | Check dependency licenses before merging any new dependency. Dependabot keeps existing deps updated. | [ADR-0009](../adr/0009-license-eupl-1.2.md) |
| R-042 | The v2 pull-mode agent introduces a new component to design, build, secure, and operate. Plan accordingly; do not block v1 on it. | M | L | **Low** | Deferred to v2. Not a v1 concern. | [ADR-0005](../adr/0005-hub-only-firewall.md) |
| R-050 | Ruleset size grows linearly with `(enabled peers × granted ports)`. At 200 peers × 20 avg granted ports = 4,000 accept rules. Performance degrades above ~10,000 rules. | L | L | **Low** | Count rules on every reload. Warn in Admin Console dashboard above configurable threshold (default 10,000). | [ADR-0006](../adr/0006-resource-level-acl.md) |
| R-052 | Resource maintenance falls entirely on the admin — no auto-discovery means every new server must be manually added as a resource. | M | L | **Low** | Materially addressed in 0.12.0: an admin-triggered discovery scan of a site's own CIDR returns a fingerprinted, bulk-importable host list ([ADR-0014](../adr/0014-device-discovery.md)). The scripted-import path remains for operators with a machine-readable inventory. Residual: discovery is best-effort (R-140). | [ADR-0006](../adr/0006-resource-level-acl.md) |
| R-140 | Discovery is **best-effort**. A host that opens none of the probed TCP ports **and** emits no ICMP port-unreachable (fully filtered, ICMP-dropping, or ICMP rate-limited — common on hardened devices and behind stateful site routers) is invisible to the scan. An operator may misread absence-from-results as "the device is not there". | M | L | **Low** | The connected-UDP probe recovers the ICMP-emitting subset; the scan UI states plainly that discovery is best-effort and that manual add is always available; the probe-port set is documented so operators know what is looked for. See §8.5 (a scan that finds nothing is a result, not an error). | [ADR-0014](../adr/0014-device-discovery.md) |
| R-141 | The scan is an authenticated **recon / port-scan primitive**. An admin — or an attacker holding a stolen admin session or an RCE (R-070) — can enumerate a remote LAN through the hub's WireGuard route. | L | M | **Low** | Admin-only; bound to the site's *declared* CIDR, so there is no free-text range and no way to point it at a network the operator has not already registered; TCP `connect()` is non-stealth and logged by the target; every scan start is audited. Ties **T-013** (§8.1), mitigation in §8.2. | [ADR-0014](../adr/0014-device-discovery.md) |
| R-142 | A large or repeatedly-triggered scan could become a **connect-flood** against the remote site network. | L | M | **Low** | Host-count cap (`/22`, ≤ 1024 hosts — larger CIDRs are rejected with 409), bounded probe concurrency, short per-host timeout, and one active scan per site. Ties **T-014** (§8.1), mitigation in §8.2. | [ADR-0014](../adr/0014-device-discovery.md) |
| R-181 | The network-diagnostics probe target must stay restricted to known Resources (never a free-text address) or the feature becomes an authenticated internal-network-**probing primitive** reachable by anyone who can reach the admin console — same recon-primitive shape as R-141. | L | M | **Low** | Target selection always resolved from Islandr's own `Resource`/`Site` data via `ResourceResource`, admin-only, every probe audit-logged (actor, target, result). See §8.1 T-018, mitigation in §8.2. | [ADR-0025](../adr/0025-network-diagnostic-helpers.md) |
| R-182 | `ping`/`tracepath`/`mtr` availability varies by host and is not guaranteed on native installs (only guaranteed inside the project-controlled `islandr-proxy` host install). An admin on a host missing `iputils` sees a degraded feature. | M | L | **Low** | Runtime detection (`GET /api/v1/diagnostics/availability`) with an actionable "install X" state in the UI rather than a silent failure or crash; the feature's absence never blocks anything else. | [ADR-0025](../adr/0025-network-diagnostic-helpers.md) |
| R-183 | A rapid, repeated probe trigger (e.g. scripted against the admin API) could become a low-grade **probe-flood** against a resource or its upstream network. | L | M | **Low** | Admin-only, rate-limited per resource (3s cooldown, mirrors Device Discovery's one-active-scan-per-site guardrail R-142/T-014); `ping` sample count fixed server-side (4), not admin-tunable. Ties **T-018** (§8.1), mitigation in §8.2. | [ADR-0025](../adr/0025-network-diagnostic-helpers.md) |
| R-184 | A leaked API key (issue #15's external-API auth) is full-admin-equivalent in v1 — no per-key scoping yet, so the blast radius of one leaked key is the whole instance. | L | M | **Low** | One-time reveal (never re-displayed after creation), SHA-256-hashed at rest, instantly revocable, per-key audit trail (created/used/revoked). Scoped keys tracked as a future refinement, not a v1 blocker. Ties **T-019** (§8.1), mitigation in §8.2. | [ADR-0026](../adr/0026-external-api-facade.md) |
| R-185 | The hand-written `docs/api/openapi.yml` can drift from the actual external-facade implementation if an endpoint changes and the spec isn't updated in the same change. | L | L | **Low** | Facade surface kept intentionally small (each addition reviewable); spec update treated as part of the same change, not a follow-up. A contract test against the spec is the natural next hardening step once the facade grows past a couple of endpoints. | [ADR-0026](../adr/0026-external-api-facade.md) |
| R-186 | *(Not yet implemented — ADR-0027 is Proposed.)* An MCP adapter would let an LLM agent call the external facade on an admin's behalf; an agent acting on ambiguous or prompt-injected instructions could trigger unintended writes (e.g. create a peer, change an ACL grant) via a valid API key before a human notices. | L | M | **Low** | No implementation exists to mitigate yet. Inherits the facade's existing key controls (one-time reveal, hashed at rest, revocable, audited — R-184). A scoped/read-only key mode is called out in ADR-0027 as a precondition to revisit before building the adapter, not an afterthought. Ties **T-020** (§8.1). | [ADR-0027](../adr/0027-mcp-server-for-llm-administration.md) |
| R-187 | *(Not yet implemented — ADR-0028 is Proposed.)* WebAuthn would use `vertx-auth-webauthn`, which the Vert.x 5 line supersedes with `vertx-auth-webauthn4j` under a non-source-compatible rename. A Quarkus platform upgrade past Vert.x 4 therefore forces a migration Islandr performs itself, where the Quarkus extension would have absorbed it. | M | L | **Low** | Bounded by construction: the engine is called from four endpoints in one package, not spread through the request path. The trade is recorded in ADR-0028 as the price of keeping a single authorization path, not as an oversight. | [ADR-0028](../adr/0028-webauthn-library-and-integration.md) |
| R-188 | *(Not yet implemented — ADR-0028 is Proposed.)* The recovery admin is the break-glass account for when OIDC is unavailable. Enforcing WebAuthn on it would turn a lost or broken authenticator into total loss of administrative access to the hub — a login-availability risk converted into a lockout. | M | H | **Medium** | Answered in the design rather than deferred (ADR-0028): registration is the opt-in, so an instance with no credential registered is unchanged; several credentials are registrable, so handover never leaves a gap; and an offline CLI command on the hub clears them, so losing every authenticator costs a shell session rather than the instance. The reset does not widen the trust boundary — whoever can run it already holds the database and the environment file — but must be audit-logged. | [ADR-0028](../adr/0028-webauthn-library-and-integration.md) |
| R-053 | Sites with many resources produce wide ACL matrix UIs; the Roles × Resources matrix becomes hard to navigate. | L | L | **Low** | Matrix is per-site (one tab per site, default to "most-granted site"), not a single table. | [ADR-0006](../adr/0006-resource-level-acl.md) |
| R-061 | A mode change from `plaintext` → `never` sets all stored key rows to NULL. Any operator who expected re-display to remain available loses all stored keys without warning. | L | M | **Low** | A one-shot CLI / admin endpoint `purge-private-keys` that NULLs the column explicitly. Documented in the operations guide. Audit entry per row. | [ADR-0007](../adr/0007-private-key-retention.md) |
| R-064 | Audit log of `PEER_CONF_RESHOW` events grows quickly when used in scripts or automation. | L | L | **Low** | Acceptable; the audit table is append-only and the retention story already exists (PRD F-15). | [ADR-0007](../adr/0007-private-key-retention.md) |
| R-111 | The `wg set wg0 *` wildcard allows any `wg set` arguments for `wg0`. A malicious caller with a shell as `islandr` could inject a crafted peer with modified allowed-IPs. | L | M | **Low** | Access as `islandr` already implies islandr is compromised — the blast radius is still bounded to WireGuard peer management on `wg0`. | [ADR-0011](../adr/0011-process-privilege-model.md) |
| R-121 | The 0.11.0 proxy JSON protocol is unauthenticated; Unix socket ownership is the only gate. Any process running as `islandr` user can send proxy commands. | L | M | **Low** | Same as R-111 — access as `islandr` already implies compromise; blast radius bounded to the WireGuard and nftables allowlist. | [ADR-0012](../adr/0012-docker-socket-proxy.md) |
| R-150 | The TLS private key, once decrypted for a `KeyStoreProvider` call, exists in JVM heap for the duration of that call. | L | M | **Low** | Encrypted at rest when `EncryptionService` is configured (same guarantee as ADR-0007 `encrypted` mode); the decrypted value is never cached or retained beyond building the `KeyStore` object. | [ADR-0015](../adr/0015-builtin-tls-termination.md) |
| R-152 | An admin-supplied malformed or mismatched certificate/key pair could be accepted and pushed live, breaking HTTPS for every client. | L | M | **Low** | Cert/key pairing and basic X.509 sanity (not expired, key usage includes server auth) validated before calling `reload()`; the previous working configuration keeps serving on validation failure. | [ADR-0015](../adr/0015-builtin-tls-termination.md) |

### Accepted (no further mitigation planned)

| ID | Risk | Rationale |
|---|---|---|
| R-041 | Lateral movement within a site is not blocked by Islandr. A peer with RDP access to Terminal-01 can use Terminal-01 as a pivot to reach other hosts in the same VLAN. | Documented limitation. Operators who need intra-site ACL configure it in UCG natively. Source: [ADR-0005](../adr/0005-hub-only-firewall.md), [ADR-0006](../adr/0006-resource-level-acl.md). |
| R-112 | Docker container deployment requires `--cap-add NET_ADMIN` and `--network host` to call `nft`/`wg` on the host, violating least-privilege. | Closed in 0.11.0: the container no longer needs `NET_ADMIN`. A host-side socket proxy (`islandr-proxy`) owns the `wg`/`nft` calls and the unprivileged container reaches it over a Unix socket ([ADR-0012](../adr/0012-docker-socket-proxy.md)); if the proxy is unreachable the hub runs in a flagged degraded mode. Residual socket-ownership and protocol risks are tracked as R-120 / R-121. Source: [ADR-0011](../adr/0011-process-privilege-model.md). |

---

## 11.2 Technical Debt

| ID | Item | Burden on | Description |
|---|---|---|---|
| TD-001 | No end-to-end firewall enforcement tests in CI | `firewall` package | `RealNftablesAdapter` is never tested in CI — only `MockNftablesAdapter` and `DryRunNftablesAdapter`. A rule generation bug that passes `nft -c -f` but produces the wrong ACL would not be caught until production. Fixing requires a CI environment with nftables and `CAP_NET_ADMIN`. |
| TD-002 | Activity geocoding schema exists but is unimplemented | `peer` package | `V27__add_geocoding.sql` added geocoding columns. No geocoding service is wired up. Columns exist in DB but are always `NULL`. PRD F-14 (show city/IP) is not satisfied. |
| TD-003 | Hand-rolled i18n | Frontend (all) | `src/main/resources/META-INF/resources/js/i18n.js` is a custom key/value store with no pluralization, no number formatting, no date formatting. Sufficient for the current German-only scope; will become friction when adding EN or additional locales. |
| TD-004 | No OpenAPI spec | REST API | The REST API has no generated OpenAPI/Swagger document. Endpoint shapes are described only in PRD §9 and integration tests. Scripting and future agents have no machine-readable contract. |
| TD-005 | Discovery scan jobs are in-memory and ephemeral | `discovery` package | Jobs live in a `ConcurrentHashMap` with a ~5-minute TTL and are lost on restart — a hub restart mid-scan drops the job and the operator re-runs it. Deliberate (a scan is cheap to repeat and does not justify a job table or a Flyway migration — [ADR-0014](../adr/0014-device-discovery.md)), but it means scan state is not observable after a restart and does not survive a multi-instance deployment. |
| TD-006 | Discovery probe-port set is hard-coded | `discovery` package | The 13 fingerprint/liveness ports are fixed in `HostProbe.DEFAULT_TCP_PORTS`; operators cannot tune them for a site that runs services on non-standard ports. Accepted for v1 ([ADR-0014](../adr/0014-device-discovery.md) §5); a configurable set is a later refinement. |
