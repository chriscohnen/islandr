# Gradle
.gradle/
.claude/
.codegraph/
build/
!gradle/wrapper/gradle-wrapper.jar

# Quarkus
target/
.quarkus/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.fleet/
.classpath
.project
.settings/
.factorypath
nbactions.xml
nb-configuration.xml

# OS
.DS_Store
Thumbs.db

# Local data (runtime SQLite dir at repo root — not src/main/resources/**/data,
# which ships bundled static assets like the world-map land outlines)
/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Secrets / local config overrides
application-local.properties
.env
.env.local
client_secret_*.json
*credentials*.json

# Logsp
*.log
hs_err_pid*

# Native build artefacts
*.dump

# Architecture diagrams — intermediate PlantUML files (PNGs are committed)
architecture/diagrams/*.puml

# Internal — planning notes (not for the public repo)
TODO.md

EUPL-1.2

.agents/*
PLAN/ipv6.md

# Transient dirs populated by islandr-site/scripts/gen-architecture.sh during
# C4 site generation (brand assets + adr-tools-formatted ADR copies). Never committed.
architecture/assets/
architecture/adr/
architecture/build/
architecture/docs/03-prd.md

# Internal design/brainstorming docs — not for the public repo
docs/superpowers/
